# this is
# a useless patch, just to test the system

CREATE TABLE false_table (
	hurg varchar(30) primary key
);

insert into false_table (hurg) values ('useless value');
insert into false_table (hurg) values ('not better');
